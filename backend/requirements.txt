# Core
fastapi
uvicorn[standard]
python-dotenv
pydantic>=2.0
pydantic-settings

# LangChain & LangGraph (let pip resolve versions)
langchain
# Pinned to 0.4.0: ragas's LangchainLLMWrapper imports
# langchain_community.chat_models.vertexai, which was removed in
# langchain-community>=0.4.1. We don't use VertexAI; this pin just keeps the
# import path RAGAS expects available.
langchain-community==0.4.0
langgraph
langchain-groq
langchain-google-genai

# Vector Database
qdrant-client
fastembed
sentence-transformers

# Database (optional for MVP - can skip initially)
# asyncpg
# sqlalchemy[asyncio]

# Persistence
langgraph-checkpoint-postgres
psycopg[binary,pool]

# Utilities
httpx
python-multipart

# Dev / Testing
pytest

# Evaluation
# Pinned to 0.3.9: ragas 0.4.x has the same langchain_community.chat_models.vertexai
# import issue as above but at a version where a matching langchain-community
# pin isn't available together with our langchain 1.x stack; 0.3.9 works with
# langchain-community==0.4.0.
ragas==0.3.9
